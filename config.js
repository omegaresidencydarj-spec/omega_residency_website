/* Frontend runtime config.
 * For GitHub Pages + separate backend:
 * set API_BASE_URL to your deployed backend, e.g. "https://omega-api.onrender.com"
 * Keep empty string for same-origin/local server mode.
 */
window.OMEGA_CONFIG = window.OMEGA_CONFIG || {};
window.OMEGA_CONFIG.API_BASE_URL = window.OMEGA_CONFIG.API_BASE_URL || '';
